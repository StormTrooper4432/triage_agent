Xylo AI Studios. Build Sprint sample data.

This is optional. Use it if it fits the workflow you pick, or bring your own real
data for your chosen vertical. It is intentionally messy: inconsistent formatting,
missing fields, typos, mixed intents. That is the point. Real client data looks
like this, not like a clean demo set.

Contents:
  emails/        14 inbound client emails, raw text, no structure
  crm_export.csv a CRM-style export. Inconsistent dates, blank fields, free-text notes.

Ideas (not requirements): triage and draft replies to the inbox, extract structured
fields from the emails, reconcile emails against the CRM, summarize each client into
an action list, classify and route by intent or urgency.
